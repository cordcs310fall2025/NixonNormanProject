-- Create the database (only run this if it doesn't already exist)
CREATE DATABASE IF NOT EXISTS nixon_norman_media;

-- Use the database
USE nixon_norman_media;

-- Create the contact form submissions table (safe to run even if you already made it)
CREATE TABLE IF NOT EXISTS contactFormSubmissions (
    id INT AUTO_INCREMENT PRIMARY KEY,
    email VARCHAR(255) NOT NULL,
    jobDate VARCHAR(255),          -- now accepts empty or any format
    selectedJobType VARCHAR(255) NOT NULL,
    jobComments TEXT
);

-- Ensure jobDate is VARCHAR (in case the table already existed)
ALTER TABLE contactFormSubmissions
MODIFY jobDate VARCHAR(255);
